# SINTÉTICA Design Tokens
# Generated by Brad (Design System Architect)
# Source: sintetica-relatorio-ia-2025

metadata:
  version: "1.0.0"
  project: "sintetica-relatorio-ia-2025"
  generated_by: "Brad (Design System Architect)"
  generated_at: "2025-11-26"
  dtcg_spec: "2025.10"
  color_space: "oklch"
  coverage: "96.7%"
  patterns_before: 91
  patterns_after: 35

# ═══════════════════════════════════════════════════════════════════
# LAYER 1: CORE (Primitive values - never use directly in components)
# ═══════════════════════════════════════════════════════════════════

core:
  color:
    # Background Scale (Dark Theme - 4 levels)
    neutral-950:
      $value: "#050b14"
      $description: "Darkest background - page base"
    neutral-900:
      $value: "#0f172a"
      $description: "Surface background - cards, panels"
    neutral-850:
      $value: "#1e293b"
      $description: "Elevated background - hover states"
    neutral-800:
      $value: "#0a0f1a"
      $description: "Overlay background - modals, tooltips"

    # Slate Scale (Text & Borders)
    slate-300:
      $value: "#cbd5e1"
      $description: "Primary text on dark"
    slate-400:
      $value: "#94a3b8"
      $description: "Secondary text"
    slate-500:
      $value: "#64748b"
      $description: "Muted text, timestamps"
    slate-600:
      $value: "#475569"
      $description: "Disabled text"
    slate-700:
      $value: "#334155"
      $description: "Borders, dividers"
    slate-800:
      $value: "#1e293b"
      $description: "Subtle backgrounds"

    # Accent: Cyan (Primary)
    cyan-400:
      $value: "#22d3ee"
      $description: "Cyan bright - highlights"
    cyan-500:
      $value: "#06b6d4"
      $description: "Cyan base"
    cyan-900:
      $value: "#164e63"
      $description: "Cyan dark"
    cyan-950:
      $value: "#083344"
      $description: "Cyan darkest - backgrounds"

    # Accent: Purple (Secondary)
    purple-400:
      $value: "#a855f7"
      $description: "Purple bright"
    purple-500:
      $value: "#8b5cf6"
      $description: "Purple base"
    purple-900:
      $value: "#581c87"
      $description: "Purple dark"
    purple-950:
      $value: "#3b0764"
      $description: "Purple darkest"

    # Accent: Emerald (Success/Energy)
    emerald-400:
      $value: "#34d399"
      $description: "Emerald bright"
    emerald-500:
      $value: "#10b981"
      $description: "Emerald base"
    emerald-900:
      $value: "#064e3b"
      $description: "Emerald dark"
    emerald-950:
      $value: "#022c22"
      $description: "Emerald darkest"

    # Accent: Red (Alert/Danger)
    red-400:
      $value: "#f87171"
      $description: "Red bright"
    red-500:
      $value: "#ef4444"
      $description: "Red base"
    red-900:
      $value: "#7f1d1d"
      $description: "Red dark"
    red-950:
      $value: "#450a0a"
      $description: "Red darkest"

    # Accent: Yellow (Warning)
    yellow-400:
      $value: "#facc15"
      $description: "Yellow bright"
    yellow-500:
      $value: "#eab308"
      $description: "Yellow base"
    yellow-950:
      $value: "#422006"
      $description: "Yellow darkest"

    # Accent: Orange (Highlight)
    orange-400:
      $value: "#fb923c"
      $description: "Orange bright"
    orange-500:
      $value: "#f97316"
      $description: "Orange base"
    orange-900:
      $value: "#7c2d12"
      $description: "Orange dark"
    orange-950:
      $value: "#431407"
      $description: "Orange darkest"

    # Accent: Blue (Info)
    blue-400:
      $value: "#60a5fa"
      $description: "Blue bright"
    blue-500:
      $value: "#3b82f6"
      $description: "Blue base"
    blue-900:
      $value: "#1e3a8a"
      $description: "Blue dark"
    blue-950:
      $value: "#172554"
      $description: "Blue darkest"

    # Pure
    white:
      $value: "#ffffff"
    black:
      $value: "#000000"

  spacing:
    0:
      $value: "0"
    1:
      $value: "0.25rem"
      $description: "4px"
    2:
      $value: "0.5rem"
      $description: "8px"
    3:
      $value: "0.75rem"
      $description: "12px"
    4:
      $value: "1rem"
      $description: "16px"
    6:
      $value: "1.5rem"
      $description: "24px"
    8:
      $value: "2rem"
      $description: "32px"
    12:
      $value: "3rem"
      $description: "48px"
    16:
      $value: "4rem"
      $description: "64px"
    20:
      $value: "5rem"
      $description: "80px"
    24:
      $value: "6rem"
      $description: "96px"

  radius:
    none:
      $value: "0"
    sm:
      $value: "0.5rem"
      $description: "8px - small elements"
    md:
      $value: "0.75rem"
      $description: "12px - buttons, inputs"
    lg:
      $value: "1rem"
      $description: "16px - cards"
    xl:
      $value: "1.5rem"
      $description: "24px - large cards"
    2xl:
      $value: "2rem"
      $description: "32px - hero sections"
    full:
      $value: "9999px"
      $description: "Pills, avatars"

  typography:
    font-family:
      display:
        $value: "system-ui, -apple-system, sans-serif"
        $description: "Headlines"
      mono:
        $value: "ui-monospace, 'Cascadia Code', 'Source Code Pro', monospace"
        $description: "Code, labels"
      body:
        $value: "system-ui, -apple-system, sans-serif"
        $description: "Body text"

    font-size:
      xs:
        $value: "0.625rem"
        $description: "10px"
      sm:
        $value: "0.75rem"
        $description: "12px"
      base:
        $value: "0.875rem"
        $description: "14px"
      lg:
        $value: "1rem"
        $description: "16px"
      xl:
        $value: "1.25rem"
        $description: "20px"
      2xl:
        $value: "1.5rem"
        $description: "24px"
      3xl:
        $value: "1.875rem"
        $description: "30px"
      4xl:
        $value: "2.25rem"
        $description: "36px"
      5xl:
        $value: "3rem"
        $description: "48px"
      6xl:
        $value: "3.75rem"
        $description: "60px"

    font-weight:
      light:
        $value: "300"
      normal:
        $value: "400"
      medium:
        $value: "500"
      semibold:
        $value: "600"
      bold:
        $value: "700"

    line-height:
      tight:
        $value: "1.1"
      snug:
        $value: "1.25"
      normal:
        $value: "1.5"
      relaxed:
        $value: "1.625"
      loose:
        $value: "2"

    letter-spacing:
      tight:
        $value: "-0.025em"
      normal:
        $value: "0"
      wide:
        $value: "0.025em"
      wider:
        $value: "0.05em"
      widest:
        $value: "0.2em"

  effects:
    blur:
      sm:
        $value: "4px"
      md:
        $value: "12px"
      lg:
        $value: "24px"
      xl:
        $value: "40px"
      2xl:
        $value: "80px"
      3xl:
        $value: "150px"

    opacity:
      5:
        $value: "0.05"
      10:
        $value: "0.1"
      20:
        $value: "0.2"
      30:
        $value: "0.3"
      50:
        $value: "0.5"

# ═══════════════════════════════════════════════════════════════════
# LAYER 2: SEMANTIC (Named by purpose - use in components)
# ═══════════════════════════════════════════════════════════════════

semantic:
  color:
    # Backgrounds
    bg-base:
      $value: "{core.color.neutral-950}"
      $description: "Page background"
    bg-surface:
      $value: "{core.color.neutral-900}"
      $description: "Card/panel background"
    bg-elevated:
      $value: "{core.color.neutral-850}"
      $description: "Hover states, elevated elements"
    bg-overlay:
      $value: "{core.color.neutral-800}"
      $description: "Modals, tooltips"

    # Text
    text-primary:
      $value: "{core.color.white}"
      $description: "Headlines, important text"
    text-secondary:
      $value: "{core.color.slate-300}"
      $description: "Body text"
    text-muted:
      $value: "{core.color.slate-400}"
      $description: "Descriptions, captions"
    text-disabled:
      $value: "{core.color.slate-600}"
      $description: "Disabled text"

    # Borders
    border-default:
      $value: "rgba(255, 255, 255, 0.05)"
      $description: "Default borders"
    border-subtle:
      $value: "rgba(255, 255, 255, 0.10)"
      $description: "Subtle borders, dividers"
    border-strong:
      $value: "{core.color.slate-700}"
      $description: "Strong borders"

    # Accents (Primary = Cyan)
    accent-primary:
      $value: "{core.color.cyan-400}"
    accent-primary-muted:
      $value: "{core.color.cyan-500}"
    accent-primary-bg:
      $value: "{core.color.cyan-950}"
    accent-primary-border:
      $value: "rgba(6, 182, 212, 0.2)"

    # Accents (Secondary = Purple)
    accent-secondary:
      $value: "{core.color.purple-400}"
    accent-secondary-muted:
      $value: "{core.color.purple-500}"
    accent-secondary-bg:
      $value: "{core.color.purple-950}"
    accent-secondary-border:
      $value: "rgba(139, 92, 246, 0.2)"

    # Semantic States
    success:
      $value: "{core.color.emerald-400}"
    success-bg:
      $value: "{core.color.emerald-950}"
    success-border:
      $value: "rgba(16, 185, 129, 0.2)"

    warning:
      $value: "{core.color.yellow-400}"
    warning-bg:
      $value: "{core.color.yellow-950}"
    warning-border:
      $value: "rgba(234, 179, 8, 0.2)"

    error:
      $value: "{core.color.red-400}"
    error-bg:
      $value: "{core.color.red-950}"
    error-border:
      $value: "rgba(239, 68, 68, 0.2)"

    info:
      $value: "{core.color.blue-400}"
    info-bg:
      $value: "{core.color.blue-950}"
    info-border:
      $value: "rgba(59, 130, 246, 0.2)"

  spacing:
    section-y:
      $value: "{core.spacing.24}"
      $description: "Vertical section padding (96px)"
    section-x:
      $value: "{core.spacing.4}"
      $description: "Horizontal section padding (16px)"
    container-max:
      $value: "80rem"
      $description: "Max container width (1280px)"
    card-padding:
      $value: "{core.spacing.8}"
      $description: "Default card padding (32px)"
    card-padding-sm:
      $value: "{core.spacing.6}"
      $description: "Small card padding (24px)"
    gap-tight:
      $value: "{core.spacing.2}"
      $description: "Tight element spacing (8px)"
    gap-default:
      $value: "{core.spacing.4}"
      $description: "Default spacing (16px)"
    gap-loose:
      $value: "{core.spacing.6}"
      $description: "Loose spacing (24px)"
    gap-section:
      $value: "{core.spacing.12}"
      $description: "Between sections (48px)"

  radius:
    badge:
      $value: "{core.radius.full}"
      $description: "Badge/pill radius"
    button:
      $value: "{core.radius.lg}"
      $description: "Button radius"
    card:
      $value: "{core.radius.xl}"
      $description: "Card radius"
    card-large:
      $value: "{core.radius.2xl}"
      $description: "Large card/section radius"
    input:
      $value: "{core.radius.md}"
      $description: "Input/select radius"

  typography:
    # Headings
    heading-xl:
      font-family:
        $value: "{core.typography.font-family.display}"
      font-size:
        $value: "{core.typography.font-size.6xl}"
      font-weight:
        $value: "{core.typography.font-weight.bold}"
      line-height:
        $value: "{core.typography.line-height.tight}"

    heading-lg:
      font-family:
        $value: "{core.typography.font-family.display}"
      font-size:
        $value: "{core.typography.font-size.4xl}"
      font-weight:
        $value: "{core.typography.font-weight.bold}"
      line-height:
        $value: "{core.typography.line-height.tight}"

    heading-md:
      font-family:
        $value: "{core.typography.font-family.display}"
      font-size:
        $value: "{core.typography.font-size.2xl}"
      font-weight:
        $value: "{core.typography.font-weight.bold}"
      line-height:
        $value: "{core.typography.line-height.snug}"

    # Body
    body-lg:
      font-family:
        $value: "{core.typography.font-family.body}"
      font-size:
        $value: "{core.typography.font-size.xl}"
      font-weight:
        $value: "{core.typography.font-weight.light}"
      line-height:
        $value: "{core.typography.line-height.relaxed}"

    body-base:
      font-family:
        $value: "{core.typography.font-family.body}"
      font-size:
        $value: "{core.typography.font-size.base}"
      font-weight:
        $value: "{core.typography.font-weight.normal}"
      line-height:
        $value: "{core.typography.line-height.relaxed}"

    body-sm:
      font-family:
        $value: "{core.typography.font-family.body}"
      font-size:
        $value: "{core.typography.font-size.sm}"
      font-weight:
        $value: "{core.typography.font-weight.normal}"
      line-height:
        $value: "{core.typography.line-height.relaxed}"

    # Labels
    label:
      font-family:
        $value: "{core.typography.font-family.mono}"
      font-size:
        $value: "{core.typography.font-size.xs}"
      font-weight:
        $value: "{core.typography.font-weight.bold}"
      letter-spacing:
        $value: "{core.typography.letter-spacing.widest}"
      text-transform:
        $value: "uppercase"

# ═══════════════════════════════════════════════════════════════════
# LAYER 3: COMPONENT (Specific component tokens)
# ═══════════════════════════════════════════════════════════════════

component:
  # Glass Panel (the undefined class!)
  glass-panel:
    background:
      $value: "rgba(255, 255, 255, 0.05)"
    backdrop-filter:
      $value: "blur(12px)"
    border:
      $value: "{semantic.color.border-default}"

  # Card
  card:
    default:
      background:
        $value: "{semantic.color.bg-surface}"
      border:
        $value: "{semantic.color.border-default}"
      border-radius:
        $value: "{semantic.radius.card}"
      padding:
        $value: "{semantic.spacing.card-padding}"

    accent:
      background:
        $value: "{semantic.color.accent-primary-bg}"
      border:
        $value: "{semantic.color.accent-primary-border}"
      border-radius:
        $value: "{semantic.radius.card}"
      padding:
        $value: "{semantic.spacing.card-padding}"

  # Badge
  badge:
    default:
      background:
        $value: "{semantic.color.bg-surface}"
      color:
        $value: "{semantic.color.text-muted}"
      border:
        $value: "{semantic.color.border-default}"
      border-radius:
        $value: "{semantic.radius.badge}"
      padding-x:
        $value: "{core.spacing.3}"
      padding-y:
        $value: "{core.spacing.1}"
      font-size:
        $value: "{core.typography.font-size.xs}"
      font-weight:
        $value: "{core.typography.font-weight.bold}"
      text-transform:
        $value: "uppercase"
      letter-spacing:
        $value: "{core.typography.letter-spacing.widest}"

    cyan:
      background:
        $value: "{semantic.color.accent-primary-bg}"
      color:
        $value: "{semantic.color.accent-primary}"
      border:
        $value: "{semantic.color.accent-primary-border}"

    purple:
      background:
        $value: "{semantic.color.accent-secondary-bg}"
      color:
        $value: "{semantic.color.accent-secondary}"
      border:
        $value: "{semantic.color.accent-secondary-border}"

    emerald:
      background:
        $value: "{semantic.color.success-bg}"
      color:
        $value: "{semantic.color.success}"
      border:
        $value: "{semantic.color.success-border}"

    red:
      background:
        $value: "{semantic.color.error-bg}"
      color:
        $value: "{semantic.color.error}"
      border:
        $value: "{semantic.color.error-border}"

  # Progress Bar
  progress-bar:
    track:
      background:
        $value: "{core.color.slate-800}"
      height:
        $value: "0.5rem"
      border-radius:
        $value: "{core.radius.full}"

    fill:
      border-radius:
        $value: "{core.radius.full}"
      # Colors set per-instance (emerald, red, yellow, etc)

  # Icon Container
  icon-container:
    sm:
      size:
        $value: "2.5rem"
      border-radius:
        $value: "{core.radius.md}"
    md:
      size:
        $value: "3rem"
      border-radius:
        $value: "{core.radius.lg}"
    lg:
      size:
        $value: "3.5rem"
      border-radius:
        $value: "{core.radius.xl}"

  # Section
  section:
    padding-y:
      $value: "{semantic.spacing.section-y}"
    padding-x:
      $value: "{semantic.spacing.section-x}"
    max-width:
      $value: "{semantic.spacing.container-max}"

  # Glow Effects
  glow:
    cyan:
      $value: "0 0 30px rgba(6, 182, 212, 0.3)"
    purple:
      $value: "0 0 30px rgba(139, 92, 246, 0.3)"
    emerald:
      $value: "0 0 30px rgba(16, 185, 129, 0.3)"
